import 'package:flutter/material.dart';

class ManageTicketsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quản lý vé'),
      ),
      body: Center(
        child: Text('Trang quản lý vé'),
      ),
    );
  }
}
