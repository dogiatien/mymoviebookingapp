import 'package:flutter/material.dart';

class ManageMoviesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quản lý phim'),
      ),
      body: Center(
        child: Text('Trang quản lý phim'),
      ),
    );
  }
}
