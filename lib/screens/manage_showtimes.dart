import 'package:flutter/material.dart';

class ManageShowtimesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quản lý lịch chiếu'),
      ),
      body: Center(
        child: Text('Trang quản lý lịch chiếu'),
      ),
    );
  }
}
